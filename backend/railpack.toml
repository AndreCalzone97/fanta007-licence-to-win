# DEPRECATED: Use the root railpack.toml instead.
# This file is kept only for reference if someone deploys with rootDirectory=/backend.
# The root railpack.toml handles the full-repo context correctly and should be the default.

[build]
cmd = "pip install -e ."

[deploy]
cmd = "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"

